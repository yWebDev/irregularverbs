/**
 * Production build metadata. Overwritten by `tools/iverbs-builders/write-build-info.cjs`
 * when you run `npm run build:prod`. Committed defaults match dev placeholders so
 * `ng build --configuration=production` alone still compiles.
 */
export const PACKAGE_VERSION = 'dev';
export const BUILD_TIME_ISO = '';
export const GIT_COMMIT = '';
